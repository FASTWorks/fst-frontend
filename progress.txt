# Progress FST Frontend

## 2026-05-09

### Initial Setup
- Created `fst-frontend` project - Inisialisasi project dengan Vite + React 19
- Created `vite.config.js` - Konfigurasi Vite dengan plugin React
- Created `src/index.css` - Setup Tailwind CSS v4 + font Geist + CSS variables theme
- Created `src/main.jsx` - Entry point React
- Created `src/lib/utils.js` - Utility `cn()` untuk class merging
- Created `components.json` - Konfigurasi shadcn/ui
- Created `eslint.config.js` - Flat config ESLint

## 2026-05-12

### Dependencies & UI Components
- Modified `package.json` - Setup dependencies (React Router, Axios, shadcn, Tailwind, dll)
- Created `src/components/ui/button.jsx` - Komponen Button shadcn dengan varian
- Created `src/components/ui/dialog.jsx` - Komponen Dialog shadcn dengan Radix
- Created `src/components/layouts/index.js` - Placeholder layout

### Login Page
- Created `src/pages/LoginPage.jsx` - Halaman login dengan form email/password

## 2026-05-14

### Routing & Pages
- Modified `src/App.jsx` - Setup React Router v7 dengan 4 routes (/, /login, /register, *)
- Modified `src/pages/index.js` - Barrel export semua halaman
- Created `src/pages/RegisterPage.jsx` - Halaman register dengan form
- Created `src/pages/NotFound.jsx` - Halaman 404

## 2026-05-15

### Homepage
- Created `src/pages/HomePage.jsx` - Halaman utama dengan hero section dan navigasi

## 2026-05-29

### API Integration — Phase 1: Foundation
- Created `src/lib/axios.js` - Axios instance dengan JWT interceptors, auto-refresh token, queue mechanism
- Created `src/lib/auth.jsx` - AuthContext + AuthProvider + useAuth hook untuk global auth state
- Created `src/components/ProtectedRoute.jsx` - Route guard component dengan branded spinner

### API Integration — Phase 2: API Modules
- Created `src/api/auth.js` - Auth API service (11 endpoint: login, register, profile, password, dll)
- Created `src/api/finance.js` - Finance API service (14 endpoint: income, transaction, receipt, saving, budget)
- Created `src/api/analytics.js` - Analytics API service (4 endpoint: summary, health, recent, insight)
- Created `src/api/aggregator.js` - Aggregator API service (1 endpoint: dashboard data)

### API Integration — Phase 3: Routing & Auth Pages
- Modified `src/App.jsx` - Wrap AuthProvider + ProtectedRoute guard untuk semua halaman terproteksi
- Modified `src/pages/LoginPage.jsx` - Integrasi form login ke API, loading/error states, redirect
- Modified `src/pages/RegisterPage.jsx` - Integrasi form register ke API, validasi client-side
- Modified `src/pages/LupaKataSandiPage.jsx` - Integrasi forgot-password API
- Modified `src/pages/AturUlangKataSandiPage.jsx` - Integrasi reset-password API dengan URL params
- Modified `src/pages/KonfirmasiPage.jsx` - Integrasi verify email + resend verification API

### API Integration — Phase 4: Dashboard & Profile
- Modified `src/pages/DashboardPage.jsx` - Fetch aggregator + AI insight, dynamic user name, logout
- Modified `src/pages/ProfilePage.jsx` - Change password, delete account, edit name, logout

### API Integration — Phase 5: Finance Pages
- Modified `src/pages/PemasukanPage.jsx` - Create income via financeApi, form submission
- Modified `src/pages/PengeluaranPage.jsx` - Create transaction via financeApi, batch submit
- Modified `src/pages/UploadPage.jsx` - OCR upload via FormData + confirm receipt API
- Modified `src/pages/BuatTabunganPage.jsx` - Create saving goal via financeApi

### FASE 2: Pemasukan — Finalisasi
- Modified `src/pages/PemasukanPage.jsx` - Auto-format Rupiah (titik ribuan) real-time di semua input currency
- Modified `src/pages/PemasukanPage.jsx` - Controlled state untuk semua input (nominalPemasukan, kebutuhanPrimer, dll)
- Modified `src/pages/PemasukanPage.jsx` - Validasi total alokasi ≤ nominal pemasukan dengan visual progress bar
- Modified `src/pages/PemasukanPage.jsx` - Cancel button navigasi ke dashboard, Save disabled saat over-allocated
- Modified `src/pages/PemasukanPage.jsx` - Payload `new_saving_goal` langsung dari state, bukan FormData

### FASE 3: Pengeluaran — Hapus Dummy + Category
- Modified `src/pages/PengeluaranPage.jsx` - Hapus dummy items (Avocado Toast, dll), init form kosong
- Modified `src/pages/PengeluaranPage.jsx` - Hapus hardcoded storeName & date, default hari ini
- Modified `src/pages/PengeluaranPage.jsx` - Tambah dropdown kategori (primer/sekunder/darurat/tabungan)
- Modified `src/pages/PengeluaranPage.jsx` - DISCARD button reset semua form state
- Modified `src/pages/PengeluaranPage.jsx` - Filter items kosong sebelum submit ke API

### FASE 4: Dashboard & Backend Fixes
- Modified `src/pages/DashboardPage.jsx` - Perbaikan path mapping dari `dashboardData` ke `dashboardData?.dashboard` sesuai respons API.
- Modified `src/pages/DashboardPage.jsx` - Memisahkan `chartData7Days` dan `chartData30Days` dengan data asli dari API (`cashflowTrend` dan `cashflowTrend30Days`).
- Modified `src/pages/DashboardPage.jsx` - Menambahkan utilitas `formatRupiah` dan `formatDate` untuk mempercantik list riwayat transaksi.
- Modified `src/components/PublicRoute.jsx` - Dibuat untuk me-redirect user yang sudah login jika mencoba mengakses halaman Home, Login, dan Register.
- Modified `src/App.jsx` - Membungkus rute `/`, `/login`, `/register`, dan halaman lupa kata sandi dengan `PublicRoute`.
- Modified `fst-aggregator-service` (Backend) - Meneruskan header `x-user-email` secara internal untuk menyelesaikan isu `401 Unauthorized`.
- Modified `fst-analytics-service` (Backend) - Memperbaiki kalkulasi `cashflowTrend` (7 hari & 30 hari) serta meningkatkan jumlah *recent transactions* yang dikirim ke frontend dari 3 menjadi 50 item.
- Modified `fst-gateway-service` (Backend) - Mengatur `rateLimitMiddleware` untuk dilewati (skip) selama berada di environment `development` untuk mencegah error HTTP 429 "Terlalu banyak permintaan".

## 2026-05-30

### FASE 4: Dashboard & Backend Fixes (Lanjutan)
- Modified `fst-analytics-service/src/services/dashboard.service.ts` - Mengubah kalkulasi grafik `cashflowTrend` dan `cashflowTrend30Days` dari akumulatif menjadi harian (*daily net*) murni.
- Modified `fst-analytics-service/src/services/dashboard.service.ts` - Mengubah kalkulasi status budget untuk mengakumulasi nilai seluruh bulan (*all-time*), serta menambahkan respons `allocTabungan`.
- Modified `src/pages/DashboardPage.jsx` - Menambahkan *progress bar* untuk kategori "Tabungan" pada area Status Budget, mengubah teks "Status Budget Bulan Ini" menjadi "Status Budget Anda".

### FASE 5: Integrasi Tabungan (Pemasukan)
- Modified `src/pages/BuatTabunganPage.jsx` - Menyederhanakan form dan menghapus fitur nabung otomatis. Form hanya menerima input `Nama Tabungan` dan `Target Tabungan`.
- Modified `src/pages/PemasukanPage.jsx` - Menambahkan dropdown semantic `<select>` untuk mengambil `tabunganList` via API agar pengguna bisa memilih target tabungan spesifik atau memilih opsi default "Total Saldo Tabungan".

### FASE 6: Perbaikan Sinkronisasi Pemasukan & Tabungan
- Modified `src/pages/PemasukanPage.jsx` - Mengembalikan toggle `isTabunganActive` dan merestorasi *state* `kebutuhanPrimer` dll yang sempat hilang.
- Modified `src/pages/PemasukanPage.jsx` - Memastikan variabel `is_saving_active` dikirim kembali di *payload* agar *backend* tidak me-reset nilai alokasi tabungan menjadi 0.
- Database Manual Fix - Memperbaiki dua row `incomes` lama dan menambah nilai tabungan di `budget_allocations` sebesar 2.050.000 untuk sinkronisasi nilai Asset vs Budget.

### FASE 7: Integrasi API Halaman Utama Tabungan
- Modified `src/App.jsx` - Memperbarui rute `/tabungan/catat` dan `/tabungan/edit` dengan menambahkan parameter dinamis `:id`.
- Modified `src/pages/TabunganPage.jsx` - Fetch data asli dari `financeApi.listSavingGoals()` untuk menggantikan data dummy.
- Modified `src/pages/TabunganPage.jsx` - Menghitung "Total Saldo Tabungan" dinamis berdasarkan *budget allocation* yang belum ditransfer.
- Modified `src/pages/TabunganPage.jsx` - Menghubungkan fungsi *Delete* tabungan ke backend API beserta *state refresh*.

### FASE 8: Integrasi API Catat Tabungan & Hotfix Backend
- Modified `src/pages/CatatTabunganPage.jsx` - Mengintegrasikan fungsi Tambah & Kurang (withdraw) ke `financeApi.addMoney`.
- Modified `src/pages/CatatTabunganPage.jsx` - Menambahkan UI validasi "Wadah Pecah" (Top-up dilarang melebihi target) dan pencegah tarik dana berlebih.
- Modified `src/api/finance.js` - Mengupdate endpoint `addMoney` agar menerima parameter `note` (keterangan).
- Modified Backend `saving-goal.service.ts` - Menambahkan kapabilitas transaksi minus (tarik tunai) serta validasi *over-target*.
- Modified Backend `saving-goal.service.ts` - Modifikasi `deleteSavingGoal` untuk me-refund `currentAmount` ke `spentTabungan` menggunakan Prisma Transaction.
- Database Manual Fix - Menjalankan script `sync-spent-tabungan.js` untuk mengembalikan saldo yang hilang karena container docker belum di-rebuild.
- System - Me-rebuild dan merestart kontainer docker `fst-finance-service` agar semua update service dapat berjalan di lokal.

### FASE 9: Perbaikan Estetika Dashboard & Logika Transaksi
- Modified `src/pages/DashboardPage.jsx` - Merapikan format tampilan daftar transaksi (menghilangkan prefix Rp ganda).
- Modified `src/pages/DashboardPage.jsx` - Mengganti ikon dan warna dinamis untuk setiap jenis transaksi (Pemasukan, Pengeluaran, Tarik Tabungan, Tambah Tabungan).
- Modified `src/pages/DashboardPage.jsx` - Menambahkan format waktu yang lebih detail (jam & menit) beserta UI catatan (note) transaksi yang rapi.
- Modified Backend `dashboard.service.ts` - Memperbaiki *analytics service* agar memasukkan *field* `note` saat me- *return* daftar riwayat transaksi terbaru.
- System - Me-rebuild dan merestart kontainer docker `fst-analytics-service` agar payload transaksi menyertakan keterangan.

### FASE 10: Integrasi API Edit Tabungan & Validasi Bisnis
- Modified `src/pages/EditTabunganPage.jsx` - Mengintegrasikan API `getSavingGoals` untuk auto-fill data dan `updateSavingGoal` untuk menyimpan perubahan target dan nama.
- Modified `src/pages/EditTabunganPage.jsx` - Menyesuaikan UI agar sesuai dengan flow MVP (menghilangkan *Nabung Otomatis* dan mengembalikan desain *dashed-container* yang elegan).
- Modified `src/pages/EditTabunganPage.jsx` & `BuatTabunganPage.jsx` - Menambahkan validasi ketat batas panjang nama tabungan (maks 38 karakter) disertai indikator sisa karakter.
- Modified `src/pages/EditTabunganPage.jsx` & `BuatTabunganPage.jsx` - Menambahkan pembatasan maksimal saldo target tabungan hingga 1 Miliar Rupiah secara *real-time*.
- Fix Frontend `EditTabunganPage.jsx` - Menyesuaikan nama parameter API payload dari *camelCase* (`goalName`) ke *snake_case* (`goal_name`) sesuai *schema* API Backend agar data tersimpan dengan benar.
- Modified `src/pages/TabunganPage.jsx` - Mengganti *alert* hapus tabungan bawaan *browser* menjadi *custom modal* elegan sesuai dengan tema Bank Jago (efek blur, warna khas, *card rounded-3xl*).
- Modified `src/pages/CatatTabunganPage.jsx` - Menerapkan validasi dinamis agar nominal penambahan tidak bisa melebihi target tabungan dan keterangan dibatasi maksimal 15 karakter dengan *live counter*.
- Modified `src/pages/CatatTabunganPage.jsx` - Menambahkan proteksi *frontend* dengan mem-*fetch* sisa *budget* agar saldo "Total Saldo Tabungan" tidak minus saat menambah dana tabungan.
- Modified Backend `saving-goal.service.ts` - Membangun validasi lapis kedua (*backend security*) untuk memblokir transaksi penambahan tabungan apabila melebihi sisa *budget* yang dialokasikan, guna menjamin "Total Saldo Tabungan" tidak pernah minus.

### FASE 11: Penyesuaian UI & Bug Fix Analytics
- Modified `src/pages/` - Mengubah semua UI label dari "Tabungan" dan "Total Saldo Tabungan" menjadi "Kantong Tabungan Utama" secara global, termasuk pada dashboard, menu sidebar, halaman pemasukan, pengeluaran, serta form edit dan catat.
- Modified Backend `dashboard.service.ts` - Memperbarui `getFinancialHealth` agar kalkulasi skor didasarkan pada proporsi Budget Allocation bulan ini (ideal 40/30/20/10) bukan sisa tabungan murni.
- Modified Backend `dashboard.service.ts` - Memperbaiki `getRecentTransactions` agar `parentCategory` dipertimbangkan bila `subCategory` tidak tersedia, sehingga pengeluaran via kategori utama tetap tercatat dan dikembalikan ke list transaksi dashboard.
- Modified `src/pages/DashboardPage.jsx` - Menampilkan Skor Kesehatan Finansial secara dinamis dengan feedback yang berasal langsung dari data backend, dan menampilkan perubahan persentase Aset dinamis secara real-time.
- Modified `src/pages/PengeluaranPage.jsx` - Memperbaiki manipulasi zona waktu (timezone) pada `transaction_date` untuk menjaga agar transaksi baru pada tanggal hari ini selalu mendapatkan stamp waktu terkini, sehingga menempati urutan teratas pada log transaksi alih-alih tenggelam karena terhitung sebagai 00:00:00.
- Modified `src/pages/ *Page.jsx` - Merevisi (revert) menu sidebar agar kembali menggunakan teks 'Tabungan' sesuai permintaan user, sementara halaman dan komponen lain tetap 'Kantong Tabungan Utama'.
- Modified Backend `dashboard.service.ts` - Memperbaiki kalkulasi skor kesehatan finansial dengan meningkatkan penalti untuk bar yang 0 (perbedaan alokasi), agar skor benar-benar merespons proporsi 40/30/20/10 dengan akurat.

### FASE 12: Validasi API Pemasukan (Backend & Frontend)
- Modified `src/pages/PemasukanPage.jsx` - Menambahkan indikator bintang (*) wajib pada label Alokasi Tabungan ketika opsi tabungan diaktifkan.
- Modified Backend `income.schema.ts` - Memperkuat validasi Zod untuk API `/incomes`. Sekarang backend akan menolak pemasukan (400 Bad Request) jika total alokasi melebihi nominal pemasukan, atau jika tabungan diaktifkan tetapi alokasi tabungannya 0.
- Modified Backend `income.schema.ts` & `income.service.ts` - Mengubah logika validasi agar total alokasi harus **persis sama (mutlak)** dengan nominal pemasukan agar data lebih presisi dan tidak ada "uang nganggur".
- Modified Frontend `PemasukanPage.jsx` - Menyesuaikan indikator *progress bar* di halaman Pemasukan untuk menuntut pengguna mendistribusikan 100% dananya, memblokir pengiriman (*submit*) jika ada sisa, beserta perubahan *alert* status.

### FASE 13: Refinement UI & Pagination
- Modified Frontend `DashboardPage.jsx` - Mengimplementasikan *Dynamic Pagination* pada daftar Transaksi Terbaru. Jika halaman melebihi 4, maka nomor halaman yang tampil hanya disederhanakan menjadi 3 nomor terdepan (relatif terhadap *current page*) dan nomor paling akhir dipisahkan dengan elipsis (`...`) agar tampilan tetap rapi.

### FASE 14: Bug Fix Sinkronisasi Asset vs Status Budget
- Database Manual Fix - Memperbaiki *desync* sebesar Rp 94.000.000 pada riwayat pemasukan lama (sebelum validasi mutlak diterapkan) yang menyebabkan "uang nganggur" (uang masuk yang tidak dialokasikan ke kategori manapun). Uang tersebut telah dialihkan otomatis ke `alloc_tabungan` (`budget_allocations`) agar sinkron.
- System Insight - Mengonfirmasi bahwa selisih Rp 50.000.000 antara **Asset** dan **Status Budget** adalah normal secara logika akuntansi, karena nilai tersebut adalah uang yang sudah "dikunci/ditransfer" ke wadah Saving Goals (Tabungan Pecahan), sehingga mengurangi *Status Budget* namun tetap menjadi bagian dari *Asset*.
## 2026-05-31

### FASE 15: Integrasi UploadPage AI OCR & Override Kategori
- Modified Backend `fst-gateway-service`:
  - Memperbaiki *bug* 502/503 di mana API Gateway secara tidak sengaja merusak *stream* `multipart/form-data` dengan memanggil `fixRequestBody` saat mengunggah gambar struk, yang menyebabkan `Unexpected end of form` di *finance service*.
- Modified Backend `fst-ai-service`:
  - Menerapkan mekanisme *Fallback/Mock Data* pada `llm_service.py` untuk mengantisipasi *HuggingFace Token* yang tidak disetel di lingkungan *development* (`.env.docker`). AI sekarang akan selalu mengembalikan data "Mock Baji Store" yang terstruktur dengan baik.
- Modified Frontend `UploadPage.jsx`:
  - Menghapus *hardcode header* `Content-Type: multipart/form-data` di `financeApi.js` agar `axios` dapat menyematkan `boundary` secara otomatis.
  - Menambahkan kapabilitas interaktif *Drag and Drop* (`onDragOver`, `onDragLeave`, `onDrop`) yang sebelumnya belum aktif.
  - Mengimplementasikan sistem *Polling Async* untuk mengecek status pemrosesan OCR AI di *backend*, sehingga UI tidak mengalami *blocking/error* saat menunggu AI bekerja.
  - Memperbarui `items` array untuk mendukung properti `category` (menarik data `aiParentCategory` hasil tebakan AI).
  - Menambahkan *Dropdown Category Select* pada *UI per-item*, memungkinkan *user* untuk meng-*override* atau mengoreksi kategori yang ditebak AI sebelum melakukan *Save Receipt*.
  - Menyesuaikan payload `financeApi.confirmReceipt` untuk menyertakan `override_parent_category` dan ID (jika item sudah ada) saat *submit*.

### FASE 16: Perbaikan UX Frontend (Sprint 1A)
- Modified `src/pages/ProfilePage.jsx` - Menambahkan fitur toggle (ikon mata) untuk melihat inputan kata sandi lama dan kata sandi baru.
- Modified `src/pages/ProfilePage.jsx` - Memperbaiki layout pesan error/sukses pada form ganti password dengan memindahkannya ke luar flex container agar *responsive* (tidak pecah di mobile).
- Modified `src/pages/LoginPage.jsx` - Menambahkan fitur toggle (ikon mata) untuk melihat inputan kata sandi.
- Modified `src/pages/ProfilePage.jsx` - Memperbaiki isu persistensi nama profil.
- Modified `src/App.jsx` - Memperbaiki konflik *routing* untuk fungsionalitas "Forgot Password".
- Modified `src/components/` - Meningkatkan responsivitas UI alerts untuk memastikan pengalaman pengguna yang lebih *seamless* dan *polished*.

### FASE 17: Proteksi Logout & Keamanan UX
- Modified `src/pages/*.jsx` - Menambahkan validasi `window.confirm` pada fungsi `handleLogout` di semua halaman (Dashboard, Profile, Tabungan, dll) untuk mencegah pengguna ter-logout secara tidak sengaja akibat salah pencet.

### FASE 18: Integrasi Edit Profil & Perbaikan API
- Modified `src/api/auth.js` - Menambahkan fungsi `updateProfile` untuk mengirim pembaruan data profil (nama) ke backend.
- Modified `src/pages/ProfilePage.jsx` - Mengganti pemanggilan `getProfile` (yang hanya mengambil data) menjadi `updateProfile` pada fitur ubah nama. Ini memperbaiki *bug* di mana nama profil yang sudah diubah akan *ter-reset* kembali ke nama lama saat berpindah ke halaman Dashboard.
- Modified *Backend* `fst-auth-service` (Manual oleh Tuan Baji) - Membuat `updateProfileController` dan mendaftarkan rute `PUT /profile` untuk menyimpan nama baru ke *Database* Prisma.

### FASE 19: Integrasi Edit Foto Profil & Keamanan Upload
- Modified `src/api/auth.js` - Menambahkan endpoint `updateProfilePicture` untuk mengirim payload JSON berisi *string* gambar Base64 tanpa perlu *multipart/form-data*.
- Modified `src/pages/ProfilePage.jsx` - Mengimplementasikan input file tersembunyi (*hidden*) yang dipicu dari klik ikon pensil. Gambar otomatis dikonversi ke Base64 via `FileReader`, disertai validasi *frontend* (maksimal 2MB, hanya ekstensi `.jpg`, `.png`, `.webp`) untuk menangkal eksploitasi unggahan *backdoor*.
- Modified `src/pages/*.jsx` (Script Otomatis) - Menyapu bersih seluruh *hardcode* gambar avatar statis "Felix"/"Baji" di 8 halaman berbeda (Dashboard, Upload, Pemasukan, dll) agar terhubung langsung dengan properti `user?.profilePicture` dari `AuthContext`.
- Modified *Backend* `fst-auth-service` (Manual oleh Tuan Baji) - Menyelaraskan *Database* dengan `npx prisma db push` untuk memuat kolom `profilePicture String?`, serta membuat `updateProfilePictureController` dengan keamanan ganda untuk menerima *string* Base64 tanpa butuh *middleware Multer*.

## 2026-06-01

### Bug Fix Backend
- Fixed `fst-analytics-service` - Menambahkan missing import `config` pada `dashboard.service.ts` yang menyebabkan build Docker gagal.
- Fixed `fst-finance-service` - Menyelesaikan *merge conflict* pada `receipt.service.ts` dan memperbaiki sintaks kurangnya tutup kurung kurawal `}` di `transaction.service.ts` yang menyebabkan proses *build* terhenti.
- Fixed `fst-finance-service` - Menghapus duplikasi deklarasi variabel `currentPeriod` pada `saving-goal.service.ts` yang menyebabkan error TypeScript (TS2451).
- Fixed `fst-ai-service` - Memperbaiki *fallback* `mock data` pada `llm_service.py` agar tetap mereturn mock jika *HuggingFace Token* kedaluwarsa atau terjadi *error* `401 Unauthorized`, alih-alih mereturn respon kosong yang menyebabkan gagal ekstraksi struk di *frontend*.
- Fixed `fst-frontend` - Mengatasi masalah *stale data* pada `DashboardPage.jsx` dengan menambahkan parameter `timestamp` *cache buster* di `aggregator.js` dan tombol Refresh manual, agar transaksi baru langsung tampil tanpa *caching* dari *browser*.
- Reverted `fst-analytics-service` - Mengembalikan logika pemetaan Uang Masuk dan Uang Keluar pada `dashboard.service.ts` ke `thisMonthIncome` dan `thisMonthExpense` sesuai permintaan (agar direset di awal bulan). Masalah utama di mana transaksi tanggal 1 tidak masuk sudah teratasi oleh perbaikan *cache buster* di *frontend* sebelumnya.
- Fixed `fst-finance-service` - Mengubah validasi saldo *budget* (kebutuhan primer, sekunder, dll) pada `transaction.service.ts` dari per-bulan menjadi **akumulatif (carry-over)** menggunakan `aggregate`. Ini menyelesaikan bug di mana saldo ratusan juta dari bulan sebelumnya dianggap tidak ada, sehingga selaras dengan total *Asset* yang ditampilkan di *frontend*.
- Fixed `fst-ai-service` - Menghapus *fallback mock data* pada `llm_service.py` sesuai permintaan *user* agar sistem murni menggunakan hasil OCR. Jika OCR gagal atau API HuggingFace bermasalah, sistem akan melemparkan *error* sebenarnya ("AI gagal membaca struk" atau "Koneksi ke AI Service gagal") alih-alih memberikan data *dummy*.
- Fixed `fst-gateway-service` - Menonaktifkan secara penuh `ocrRateLimitMiddleware` dan `rateLimitMiddleware` untuk tahap pengembangan, sehingga Tuan Baji bisa melakukan tes unggah gambar tanpa batas tanpa terblokir *error* "Koneksi gagal".
- Fixed `fst-finance-service` - Memodifikasi `receipt.service.ts` agar menangkap pesan *error* sebenarnya dari *AI service* (seperti *rate limit* bawaan Hugging Face atau OCR gagal) dan meneruskannya ke *frontend*, sehingga pesan *error* tidak lagi terpaku pada tulisan *hardcode* "Koneksi ke AI Service gagal".

### FASE 20: Bug Fix OCR, Dashboard & Penanganan Limit (Frontend)
- Fixed `fst-frontend/src/api/finance.js` - Memperbaiki gagal unggah OCR (*Receipt image is required*) dengan secara eksplisit mengatur `Content-Type: multipart/form-data` agar Axios tidak mengirimkannya sebagai JSON.
- Modified `src/pages/DashboardPage.jsx` - Menambahkan fitur Modal Edit Budget secara dinamis, lengkap dengan tombol "Hitung Otomatis" (50/30/10/10) berdasarkan nilai Aset, sehingga pengguna dapat mengisi porsi budget yang masih `0`.
- Fixed `src/pages/PengeluaranPage.jsx` - Menambahkan *graceful error handling* untuk mencegat error `HTTP 429 Too Many Requests` (akibat *rate limit* bawaan *Gateway* Railway), sehingga muncul *alert* yang jelas ke pengguna alih-alih merusak tampilan.
- Fixed `src/pages/DashboardPage.jsx` - Memperbaiki *bug* "Asset tiba-tiba 0" dengan memastikan state `dashboardData` tidak di-*reset* ke `null` atau `0` saat terjadi kegagalan permintaan (termasuk error `429`).
- Fixed `src/pages/DashboardPage.jsx` - Memperbaiki Line Chart Tren Saldo (Cashflow) agar tampil dinamis menggunakan `financeApi` sebagai *fallback* penghitungan histori saldo (kumulatif), sehingga chart naik saat ada uang masuk dan turun saat ada uang keluar. Mengatur ulang *padding* titik koordinat *chart* agar garis tidak terpotong di sisi bawah.
- Modified Frontend CatatTabunganPage.jsx - Ensure finalAmount is strictly parsed as Number and negative for tarik tunai, blocking exact zero.
- Fixed Backend saving-goal.service.ts - Memperbaiki isu "Kantong Tabungan Utama tidak kembali" dengan memastikan budgetAllocation bulan ini dibuat terlebih dahulu (menggunakan getOrCreateBudgetWithRollover) sebelum diupdate/refund, guna menghindari updateMany yang gagal secara diam-diam.

### Audit & Planning: Full API Documentation Review
- Created `integration_plan.md` - Audit menyeluruh API_DOCUMENTATION.md vs codebase frontend. Mengidentifikasi 4 bug kritis di Dashboard (Tren Saldo, Status Budget, Transaksi Terbaru, Hapus Tabungan). Membuat gap analysis 25+ endpoint terintegrasi vs 11 endpoint yang belum/bermasalah. Menyusun roadmap integrasi 6 fase dengan 15 task terurut prioritas.

### INTEGRATION PHASE 1: Fix Dashboard Fallback Logic & Analytics
- Modified `DashboardPage.jsx` - Merombak perhitungan Dashboard untuk membaca *real-time API endpoint*:
  1. **Cashflow (Tren Saldo)**: Mengubah konsep menjadi **Backward Asset Tracking**. Titik grafik terakhir secara mutlak selalu terkunci pada angka `totalAsset` dari backend. Sedangkan titik-titik pada hari sebelumnya dihitung *mundur* berdasarkan riwayat arus kas murni (`netChange` harian). Hal ini memastikan dua syarat kritis terpenuhi: (1) Grafik benar-benar mengacu pada Saldo Aset yang sebenarnya (misal: 120 Juta), dan (2) Data pada hari-hari kemarin bersifat statis dan terkunci selamanya tanpa bisa bergeser, karena hanya direkonstruksi dari transaksi harian historis.
  2. **Transaksi Terbaru**: Mengganti fallback lokal manual dengan pemanggilan langsung ke endpoint `GET /api/analytics/recent` dan meningkatkan UX tampilan dengan default **10 item per halaman** sesuai instruksi.
  3. **FAST AI Insight**: Menghubungkan komponen *insight* dengan endpoint `GET /api/analytics/insight` dan menambahkan fungsi Regex untuk otomatis memformat angka yang dihasilkan AI dengan titik pemisah ribuan (misal: 3000000 menjadi 3.000.000).
- Modified `DashboardPage.jsx` - Menambahkan `.catch()` pada pemanggilan API `listIncomes` dan `listTransactions` untuk mencegah *Promise.all* melempar *fatal error* (blank/0 array) jika endpoint sedang *down* atau kosong.
- Modified `DashboardPage.jsx` - Memperbaiki parsing array response `.data?.data` untuk list incomes dan transactions yang gagal di-_render_.
- Modified `DashboardPage.jsx` - Merombak kalkulasi *Tren Saldo (Cashflow)* menjadi *Running Balance (Kumulatif)* yang akurat, dengan mengakumulasikan Pemasukan dan Pengeluaran sesuai riwayat transaksi sesungguhnya, bukan sekadar selisih harian.
- Modified `DashboardPage.jsx` - Memperbaiki Bug *Status Budget* dengan menggunakan response langsung dari API `GET /api/finance/budget/summary`, serta menyesuaikan field schema backend (`alloc_kebutuhan_primer` dst) agar data progress bar budget 100% tersinkron dengan alokasi.
- Modified `DashboardPage.jsx` - Menyempurnakan *Transaksi Terbaru* dengan menggabungkan data pengeluaran dan pemasukan secara dinamis, sehingga Pemasukan kini muncul di *List Transaksi Terbaru* dengan ikon dan format penanda (+).

### INTEGRATION PHASE 2: Fix Tabungan Saldo Return
- Modified `TabunganPage.jsx` - Menambahkan notifikasi pengembalian saldo ketika pengguna menghapus (delete) tabungan. Alert kini secara eksplisit menampilkan nilai nominal (*currentAmount*) yang dikembalikan ke Kantong Tabungan Utama secara otomatis (sejalan dengan perbaikan Prisma Transaction dari Backend di fase sebelumnya).

### INTEGRATION PHASE 3 (Tabungan Adjustments)
- Modified `EditTabunganPage.jsx` - Mengoreksi _wording_ form edit dari "Kantong Tabungan Utama" menjadi "Tabungan" agar tidak membingungkan pengguna.
- Fixed `EditTabunganPage.jsx` - Mengatasi *bug* "blank white screen" (React ReferenceError) yang disebabkan oleh variabel `user` yang dipanggil di elemen *header* namun belum di-*destructure* dari `useAuth()`. Endpoint `PUT /api/finance/saving-goals/{id}` kini dipastikan berjalan mulus 100%.
